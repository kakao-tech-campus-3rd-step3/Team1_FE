export interface User {
  id: string;
  name: string;
  avatar?: string;
  createdAt?: Date;
  updatedAt?: Date;
}

export interface Member {
  id: string;
  name: string;
  avatar?: string;
  createdAt?: Date;
  updatedAt?: Date;
}
